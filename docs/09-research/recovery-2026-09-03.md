# Release-Candidate Recovery — 3 September 2026

## Why recovery was required

An earlier private handoff recorded an unpublished candidate commit and Git bundle. The commit was not present in the canonical GitHub repository, the maintainer's RC2 clone or any retained bundle. Its claimed SHA therefore cannot be used as a release identity.

## Recovery basis

Recovery starts from the published and signed RC2 history at commit `6ccdb0230fe3c025e13b5aac4348e4233813613b`. No tag is moved. The recovered candidate receives a new commit SHA and is accepted only from its own clean tree and CI result.

## Reconstructed changes

The recovery implements the documented requirements rather than attempting to counterfeit the lost commit:

- public identity changed to **LNG Carrier Virtual Engineering Lab**;
- preflight made independent of the caller's working directory;
- process, coordinator and alarm health routes fail closed while unready;
- HTTP reachability no longer treats 404 or server errors as success;
- process-dependent Compose services wait for healthy model services;
- verified historian configuration forces a visible Telegraf recreation;
- mutable FUXA application state is kept out of Git;
- complete-vessel wording is replaced by an explicit cross-domain scope;
- machine-readable scope, architecture, vessel-coverage, data-semantics, timebase, OPC UA security and detection-claim boundaries are added;
- repeated-run aggregation and checksum-indexed Gates A–G dossier tooling are added;
- the quality, research, deep-review and test gates enforce the recovered contracts.

## Validation boundary

The recovered candidate is statically validated and ready for GitHub CI. Docker/OpenPLC/OPC UA/FUXA/InfluxDB commissioning, repeated experiments and network-fidelity Gates NF-A–NF-G remain pending target-server evidence. Recovery does not turn those pending claims into passes.
